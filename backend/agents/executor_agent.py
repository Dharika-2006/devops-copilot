# STUB — replace with T3's real file when they share it
class ExecutorAgent:
    async def run(self, session_id: str, plan, approval_token: str):
        raise NotImplementedError("Waiting for T3's executor_agent code")

executor_agent = ExecutorAgent()