# STUB — replace with T3's real file when they share it
class SafetyAgent:
    async def run(self, session_id: str, plan):
        raise NotImplementedError("Waiting for T3's safety_agent code")

safety_agent = SafetyAgent()